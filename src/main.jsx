import React from 'react';
import ReactDOM from 'react-dom/client';
import DailyOS from './DailyOS.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <DailyOS />
  </React.StrictMode>
);
